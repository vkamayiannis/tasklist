<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label t :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="form-group">
    <%= f.label t :started %><br>
    <%= f.text_field :started, class: "datetimepicker" %>
  </div>
  <div class="form-group">
    <%= f.label t :finished %><br>
    <%= f.text_field :finished, class: "datetimepicker" %>
  </div>
  <div class="form-group">
    <%= f.label t :allday %><br>
    <%= f.check_box :allday %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
